# AI育种项目申报书与预算智能生成系统

## 项目简介

本系统是一个高度动态、可扩展的AI辅助申报书（本子）及预算表生成系统。核心聚焦于"AI育种"领域，涵盖作物、园艺、禽类、食用菌等，且物种分类支持动态无限扩展。

系统通过"数据采集 → 实体关联 → 动态Prompt组装 → LLM内容生成 → 预算汇总"的闭环，实现复杂科研申报书的自动化、模块化撰写与管理。

## 技术栈

### 前端
- Vue 3 + TypeScript
- Element Plus UI组件库
- Vite 构建工具
- Pinia 状态管理
- Vue Router 路由

### 后端
- Python FastAPI
- SQLAlchemy Async (异步ORM)
- PostgreSQL 数据库
- Pydantic 数据验证

### AI能力
- 对接大模型API进行智能内容生成
- 支持流式输出
- 版本历史与内容优化

## 核心功能

### 1. 项目管理
- 多项目管理
- 用户角色权限控制（填报人、审核员、管理员、超级管理员）

### 2. 数据采集系统
- 场景管理
- 设备管理
- 数据集管理
- AI模型管理
- 研发项目管理

### 3. 大纲编辑系统
- 动态树形大纲结构
- 节点内容编辑
- 引用管理（核心引用、参考背景、预算关联）
- AI内容生成
- 内容版本历史
- 内容优化（润色、扩展、精简、正式）

### 4. 预算汇总
- 设备预算
- 数据集预算
- AI模型预算
- 研发项目预算
- 预算导出

### 5. 文档导出
- Word文档导出
- 支持自定义格式（字体、页眉页脚、页码等）

## 项目结构

```
breeding/
├── backend/                    # FastAPI后端
│   ├── app/
│   │   ├── api/               # API路由
│   │   │   ├── auth.py        # 认证接口
│   │   │   ├── outline.py     # 大纲接口
│   │   │   ├── data_collection.py  # 数据采集接口
│   │   │   ├── budget.py      # 预算接口
│   │   │   └── doc_agent.py   # AI生成接口
│   │   ├── models/             # 数据模型
│   │   │   ├── user.py         # 用户模型
│   │   │   ├── outline.py      # 大纲模型
│   │   │   └── data_collection.py  # 数据采集模型
│   │   ├── schemas/            # Pydantic模型
│   │   ├── core/               # 核心配置
│   │   │   └── database.py     # 数据库配置
│   │   └── utils/              # 工具函数
│   │       └── doc_exporter.py # Word导出工具
│   └── main.py                 # 应用入口
├── frontend/                   # Vue 3前端
│   ├── src/
│   │   ├── api/               # API调用
│   │   ├── views/             # 页面组件
│   │   │   ├── Login.vue      # 登录页
│   │   │   ├── Projects.vue   # 项目列表
│   │   │   ├── Outline.vue    # 大纲编辑
│   │   │   ├── DataCollection.vue  # 数据采集
│   │   │   └── Budget.vue     # 预算管理
│   │   ├── components/        # 通用组件
│   │   ├── router/            # 路由配置
│   │   └── stores/            # Pinia状态
│   └── vite.config.ts         # Vite配置
├── database/                   # 数据库文件
└── *.md                       # 项目文档
```

## 快速开始

### 环境要求
- Node.js 18+
- Python 3.9+
- PostgreSQL 14+

### 后端启动

```bash
cd backend
pip install -r requirements.txt
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 前端启动

```bash
cd frontend
npm install
npm run dev
```

### 访问地址
- 前端: http://localhost:3000
- 后端API文档: http://localhost:8000/docs

## 开发规范

1. **大纲结构** - 必须通过数据库配置，不可在前端硬编码
2. **Prompt管理** - 所有Prompt模板存放在独立文件中，支持动态修改
3. **物种扩展** - 通过字典管理模块支持动态添加新物种
4. **预算追溯** - 预算汇总支持反向追溯

## 文档

- [本子结构.md](./本子结构.md) - 申报书大纲结构
- [数据采集.md](./数据采集.md) - 数据采集流程说明
- [文档格式要求.md](./文档格式要求.md) - Word导出格式要求
- [background.md](./background.md) - 项目背景

## 许可证

MIT License
